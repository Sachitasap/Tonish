# Tonish Hybrid AI Architecture

```
╔════════════════════════════════════════════════════════════════════════╗
║                         TONISH HYBRID AI STACK                         ║
║                    IP: 192.168.5.10 (All Services)                     ║
╚════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│                          USER BROWSER                                    │
│                     http://192.168.5.10:5173                            │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                                 │ HTTP Requests
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    FRONTEND (SvelteKit)                                  │
│                         Port: 5173                                       │
├─────────────────────────────────────────────────────────────────────────┤
│  Components:                                                             │
│   • MagicWand.svelte ────────────────────────┐                          │
│   • +page.svelte (myflow, myflowbook)         │                         │
│                                                │                         │
│  API Layer (src/lib/api.ts):                  │                         │
│   • taskAPI                                    │                         │
│   • notebookAPI                                │                         │
│   • aiAPI ◄──────────────────────────────────┘                          │
│     - enhanceTask()                                                      │
│     - breakdownTask()                                                    │
│     - analyzeNotebook()                                                  │
│     - generatePageIdeas()                                                │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                                 │ JSON API Calls
                                 │ http://192.168.5.10:8080/api
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      BACKEND (Go + Fiber)                                │
│                          Port: 8080                                      │
├─────────────────────────────────────────────────────────────────────────┤
│  Routes (routes/routes.go):                                              │
│   /api/tasks/*          → handlers/task.go                              │
│   /api/notebooks/*      → handlers/notebook.go                          │
│   /api/ai/*            → handlers/ai.go ◄─── Magic Wand Endpoints       │
│                                                                           │
│  AI Handlers (handlers/ai.go):                                           │
│   POST /api/ai/enhance-task ───────┐                                    │
│   POST /api/ai/breakdown-task       │                                   │
│   GET  /api/ai/analyze-notebook/:id │                                   │
│   GET  /api/ai/generate-page-ideas  │                                   │
│   GET  /api/ai/health              │                                    │
│                                     │                                    │
│  ┌──────────────────────────────────▼───────────────────────────────┐  │
│  │              AI Service Layer (ai/)                               │  │
│  │                                                                    │  │
│  │  toon.go (TOON Converter):                                        │  │
│  │   • TaskToTOON()      ─┐                                          │  │
│  │   • NotebookToTOON()   ├─► Token-Oriented Object Notation        │  │
│  │   • TOONPrompt()       │   (Structured AI-friendly format)        │  │
│  │   • ParseAIResponse() ─┘                                          │  │
│  │                                                                    │  │
│  │  ollama.go (Ollama Client):                                       │  │
│  │   • NewOllamaClient()                                             │  │
│  │   • Generate(prompt) ───► HTTP POST to Ollama                     │  │
│  │   • HealthCheck()                                                 │  │
│  └────────────────────────────┬───────────────────────────────────────┘  │
│                                │                                          │
│  Database (database/database.go):                                        │
│   • SQLite (tonish.db)                                                   │
│   • GORM Models                                                          │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                                 │ HTTP POST
                                 │ TOON Format Prompt
                                 │ http://192.168.5.10:11434/api/generate
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                   OLLAMA (External Service)                              │
│                        Port: 11434                                       │
├─────────────────────────────────────────────────────────────────────────┤
│  API Endpoints:                                                          │
│   /api/generate  ◄── Main endpoint for AI processing                    │
│   /api/chat                                                              │
│   /api/tags                                                              │
│   /api/pull                                                              │
│                                                                           │
│  Model: Qwen 2.5-Coder                                                   │
│   • Specialized for code & structured data                              │
│   • Processes TOON format                                               │
│   • Returns JSON responses                                              │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                                 │ JSON Response
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    Response Flow (Back Up)                               │
├─────────────────────────────────────────────────────────────────────────┤
│  1. Ollama returns JSON                                                  │
│  2. Backend parses with ParseAIResponse()                               │
│  3. Database updated (if needed)                                        │
│  4. Enhanced data sent to Frontend                                      │
│  5. MagicWand.svelte displays preview                                   │
│  6. User applies changes                                                │
│  7. Final update to database                                            │
└─────────────────────────────────────────────────────────────────────────┘


╔════════════════════════════════════════════════════════════════════════╗
║                         DATA FORMATS                                    ║
╚════════════════════════════════════════════════════════════════════════╝

Frontend → Backend (JSON):
────────────────────────────
{
  "title": "Fix login bug",
  "description": "Users can't log in",
  "priority": "medium"
}

Backend → Ollama (TOON Format):
────────────────────────────────
TASK_OBJECT {
  title: "Fix login bug"
  description: "Users can't log in"
  priority: medium
  status: todo
}

Prompt Context:
You are Tonish AI Assistant.
Action: enhance_task
[TOON data above]
Respond with valid JSON only:
{"title": "...", "description": "...", "priority": "...", ...}

Ollama → Backend (JSON):
─────────────────────────
{
  "model": "qwen2.5-coder:latest",
  "response": "{\"title\": \"Fix critical authentication bug...\", ...}"
}

Backend → Frontend (Parsed JSON):
──────────────────────────────────
{
  "enhanced": {
    "title": "Fix critical authentication bug in user login",
    "description": "Investigate and resolve...",
    "priority": "high",
    "quadrant": "urgent-important",
    "tags": ["bug", "authentication", "security"],
    "suggestions": "Add error logging..."
  },
  "original": { ... }
}


╔════════════════════════════════════════════════════════════════════════╗
║                       NETWORK TOPOLOGY                                  ║
╚════════════════════════════════════════════════════════════════════════╝

192.168.5.10
    │
    ├── :5173 ────► Frontend (SvelteKit in Docker)
    │
    ├── :8080 ────► Backend (Go/Fiber in Docker)
    │                   │
    │                   └──► tonish-network (Docker bridge)
    │
    └── :11434 ───► Ollama (External/Existing)
                        │
                        └──► qwen2.5-coder:latest


╔════════════════════════════════════════════════════════════════════════╗
║                    MAGIC WAND USER FLOW                                 ║
╚════════════════════════════════════════════════════════════════════════╝

1. User creates task: "Fix bug"
   │
   ▼
2. User clicks "✨ Magic Wand" button
   │
   ▼
3. MagicWand.svelte shows loading spinner
   │
   ▼
4. Frontend calls: aiAPI.enhanceTask(task)
   │
   ▼
5. Backend receives JSON, converts to TOON
   │
   ▼
6. Ollama processes with Qwen 2.5-Coder (2-5 sec)
   │
   ▼
7. Backend receives enhanced data
   │
   ▼
8. Frontend displays preview panel:
   ┌──────────────────────────────────┐
   │ ✨ AI Enhancement Preview        │
   ├──────────────────────────────────┤
   │ Title: Fix critical auth bug...  │
   │ Priority: HIGH                   │
   │ Quadrant: urgent-important       │
   │ Tags: [bug, security, auth]      │
   │ 💡 Add error logging...          │
   │                                  │
   │ [✓ Apply] [✗ Cancel]             │
   └──────────────────────────────────┘
   │
   ▼
9. User clicks "Apply"
   │
   ▼
10. Task updated in database
    │
    ▼
11. UI refreshes with enhanced task


╔════════════════════════════════════════════════════════════════════════╗
║                      DEPLOYMENT OPTIONS                                 ║
╚════════════════════════════════════════════════════════════════════════╝

Production (Docker Compose):
────────────────────────────
$ docker-compose up -d
  ├── frontend:5173
  ├── backend:8080
  └── Uses external Ollama at 192.168.5.10:11434

Development Mode:
─────────────────
$ ./start-dev.sh
  ├── Frontend: npm run dev (Vite)
  ├── Backend: go run main.go
  └── Ollama: External at 192.168.5.10:11434

Environment Variables:
──────────────────────
PORT=8080
OLLAMA_URL=http://192.168.5.10:11434
OLLAMA_MODEL=qwen2.5-coder:latest


╔════════════════════════════════════════════════════════════════════════╗
║                         FILE STRUCTURE                                  ║
╚════════════════════════════════════════════════════════════════════════╝

Tonish/
├── backend/
│   ├── ai/ ◄────────────────────────── NEW: AI Services
│   │   ├── ollama.go                   (Ollama HTTP client)
│   │   └── toon.go                     (TOON converter + prompts)
│   ├── handlers/
│   │   ├── ai.go ◄──────────────────── NEW: Magic Wand endpoints
│   │   ├── auth.go
│   │   ├── notebook.go
│   │   └── task.go
│   ├── routes/
│   │   └── routes.go ◄──────────────── UPDATED: AI routes added
│   ├── main.go ◄────────────────────── UPDATED: AI initialization
│   └── Dockerfile ◄─────────────────── UPDATED: Port 8080
│
├── frontend/
│   └── src/lib/
│       ├── api.ts ◄──────────────────── UPDATED: aiAPI added
│       └── components/
│           └── MagicWand.svelte ◄───── NEW: AI UI component
│
├── docker-compose.yml ◄────────────────── UPDATED: External Ollama config
├── start-dev.sh ◄──────────────────────── UPDATED: New ports + AI
├── setup-ai.sh ◄───────────────────────── NEW: Automated setup
├── test-ollama.sh ◄────────────────────── NEW: Connection test
│
└── Documentation/
    ├── SETUP_COMPLETE.md ◄─────────────── Quick start
    ├── OLLAMA_SETUP.md ◄───────────────── Main guide
    ├── AI_INTEGRATION_GUIDE.md          Full docs
    └── AI_CONFIGURATION_SUMMARY.md      Config reference
